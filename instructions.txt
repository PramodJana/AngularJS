installation steps
1) install nodejs
2) install angularcli
	a)npm install -g @angular/cli

3)creating project
	a)ng new projectname(mean-course)

4)run the command "ng serve"

5)Installing angular material
	a)ng add @angular/material(if using latest version) or npm install -g @angular/cli



how to create component
ng g c test(COMPONENT NAME)